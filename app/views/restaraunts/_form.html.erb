<%= form_for(@restaraunt) do |f| %>
  <% if @restaraunt.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@restaraunt.errors.count, "error") %> prohibited this restaraunt from being saved:</h2>

      <ul>
      <% @restaraunt.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_area :name %>
  </div>
  <div class="field">
    <%= f.label :address %><br>
    <%= f.text_area :address %>
  </div>
  <div class="field">
    <%= f.label :rating %><br>
    <%= f.number_field :rating %>
  </div>
  <div class="field">
    <%= f.label :is_open %><br>
    <%= f.check_box :is_open %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
